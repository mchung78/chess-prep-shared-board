{
  "name": "chess-prep-shared-board",
  "version": "1.0.0",
  "private": true,
  "description": "Rules test tooling for the shared board's Firebase Realtime Database rules (issue #388)",
  "scripts": {
    "test:rules": "firebase emulators:exec --only database \"mocha test/rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.35.1",
    "mocha": "^10.7.3"
  }
}
